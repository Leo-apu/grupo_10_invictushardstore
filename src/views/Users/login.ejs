<!DOCTYPE html>
<html lang="es">

<head>
    <%- include('../partials/head.ejs') %>
        <link rel="icon" href="/images/logo-InvictusHardStore-1.png">
        <link rel="stylesheet" href="../css/home.css">
        <link rel="stylesheet" href="../css/login.css">
        <link rel="stylesheet" href="../css/index.css">
        <title>Invictus Hard Store</title>
</head>

<body>
    <header>
        <%- include('../partials/header.ejs') %>
    </header>

    <main>
        <div class="form-container-login">
            <p class="title">INICIAR SESION</p>
            <p class="signup">Inicie Sesion con su Cuenta de
                <span>Invictus Hard Store</span>
            </p>
            <form class="form" method="POST" action="/users/login">
                <div class="input-group">
                    <input required class="input" type="text" name="email" id="email" placeholder="Email">
                    <% if (locals.errors && errors.email) { %>
                        <div style="color: red"><%= errors.email.msg %></div>

                        <% } %>
                </div>
                <div class="input-group">
                    <input required class="input" type="password" name="password" id="password" placeholder="Contraseña">
                    <% if (locals.errors && errors.password) { %>
                        <div style="color: red"><%= errors.password.msg %></div>

                        <% } %>
                </div>
                <div class="flex-row">
                    <div>
                        <input type="checkbox" id="check" name="remember">
                        <label class="remember" for="check">Recuerdame </label>
                    </div>
                </div>
                <button class="sign" type="submit">INGRESAR</button>
                
                
            </form>
                <a class="registerButtonl" href="/users/register">
                    <button class="register">CREAR CUENTA</button>
                </a>
                <div class="forgot">
                    <a rel="noopener noreferrer" href="#">¿ Olvidaste la contraseña ?</a>
                </div>
            <p class="signup">
                Si tienes problemas para iniciar sesion escribemos a feedback_soporte@invictushardstore.com
            </p>
        </div>
    </main>

    <footer>
        <%- include('../partials/footer.ejs') %>
    </footer>

</body>

</html>
